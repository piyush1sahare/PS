# Sorry page for Neha ❤️

A single-file static apology page for GitHub Pages.

## Publish
Upload `index.html` and `.nojekyll` to a GitHub repository and enable GitHub Pages from the repository's **Settings → Pages**.

The page is fully static: HTML, CSS and JavaScript are all contained in `index.html`.
